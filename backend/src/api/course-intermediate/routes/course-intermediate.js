'use strict';

/**
 * course-intermediate router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::course-intermediate.course-intermediate');
