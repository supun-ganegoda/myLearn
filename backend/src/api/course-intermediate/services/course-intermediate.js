'use strict';

/**
 * course-intermediate service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::course-intermediate.course-intermediate');
